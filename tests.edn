#kaocha/v1
 {:tests
  [{:id :unit
    :source-paths ["src"]
    :test-paths ["test/src"]
    :skip-meta [:skip]
    :focus-meta [:focus]}]

  :cloverage/opts
  {:codecov? true}}
   ; :coveralls? true
   ; :emma-xml? true
   ; :junit? true
   ; :lcov? true
   ; :raw? true}}
